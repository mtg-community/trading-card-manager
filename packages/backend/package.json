{
  "name": "backend",
  "private": true,
  "version": "0.0.1",
  "description": "Firebase Hosting and Functions",
  "engines": {
    "node": "8"
  },
  "scripts": {
    "login": "firebase login --reauth",
    "deploy": "firebase deploy"
  }
}
