import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  manaContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 100,
    padding: 3,
    marginHorizontal: 1,
  },
  mana: {
    fontSize: 12,
    fontFamily: 'Mana',
  },
});
