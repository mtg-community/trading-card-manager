install:
	yarn install

test:
	yarn test
	yarn types:check
	yarn lint
	yarn prettier:check

build:
	yarn build-web

sonarqube:
	echo 'Sonarqube'
#	@sonar-scanner \
#		-Dproject.settings=sonar-scanner.properties \
#    -Dsonar.login=${SONAR_CLOUD_TOKEN}

deploy:
	echo 'Deploy'
