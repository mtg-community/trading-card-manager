import React from 'react'
import { FormattedMessage } from 'react-intl';

export const Private = ({user}) =>{
    return (
      <h1>
        <FormattedMessage
          id='private.title'
        >
        </FormattedMessage>
      </h1>
    );
};
