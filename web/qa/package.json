{
  "name": "web-qa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd .. && PORT=3030 npm start",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "percy:run": "percy exec -- cypress run --browser chrome --spec 'cypress/integration/percy/*.spec.js'",
    "test": "npm start & wait-on http://localhost:3030 && cypress:run"
  },
  "dependencies": {},
  "devDependencies": {
    "@percy/cypress": "0.2.3",
    "cypress": "3.1.4",
    "wait-on": "3.2.0"
  }
}
